# Optional: Override the default upstream talker_service URL.
# At runtime, this is overridden by the MCM config.sync from the game.
# Only useful for standalone bridge testing without the game running.
# SERVICE_WS_URL=wss://talker-live.duckdns.org/ws
