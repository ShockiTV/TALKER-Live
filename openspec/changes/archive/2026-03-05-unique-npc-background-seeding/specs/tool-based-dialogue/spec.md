## NO MODIFICATIONS

This change does not modify the `tool-based-dialogue` spec. All background seeding is handled entirely on the Lua side as a persistence-layer migration. The Python-side `_handle_background` handler, system prompt, and tool definitions remain unchanged.
